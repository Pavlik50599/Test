using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Opamusarok : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
       //Bedi s bashkoy 
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
